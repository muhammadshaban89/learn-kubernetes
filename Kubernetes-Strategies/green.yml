kind: Deployment
apiVersion: apps/v1
metadata:
   name: green-deploy
spec:
   replicas: 2
   selector:     
    matchLabels:
      app: apache
      ver: green
   template:
     metadata:
       labels:
         app: apache
         ver: blue
     spec:
      containers:
        - name: webcon
          image: apache
